#include<stdio.h>
#include<conio.h>

// Printing sting using puts function

int main(){
    char name[10];
    printf("Enter name: ");
    gets(name);
    printf("Entered name is: ")
    puts(name);

getch();
return 0;
}
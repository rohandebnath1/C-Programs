#include <stdio.h>
#include <conio.h>

// Area of a circle

int main(){
    float radius;
    printf("Enter Radius:");
    scanf("%f",&radius);

    printf("Area is : %.2f", 3.14 * radius * radius);
    return 0;
getch();
}
#include<stdio.h>

// Initialisation of 2-D Array and accessing last elements

int main(){
    int arr[2][2] = {1,0,2,5};
    printf("The last element of 2-D array is as follows: %d",arr[1][1]);
return 0;
}